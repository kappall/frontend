name: frontend
description: "Frontend of MySchedule app"

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0
  
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^16.2.4
  provider: ^6.1.5+1
  http: ^1.5.0
  flutter_secure_storage: ^9.2.4
  dio: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true